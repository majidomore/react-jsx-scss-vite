import React from "react"
import logo from '@/assets/img/logo.svg'

export const Header = ({}) => (
    <div>
        <img src={logo} className="App-logo" alt="logo" />
        <h2>Header</h2>
    </div>
)